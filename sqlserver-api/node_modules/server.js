const express = require('express');
const sql = require('mssql');

const app = express();
const port = process.env.PORT || 1433; //3000

const dbConfig = {
  user: 'PUCHotMap',
  password: 'PUCMINASCalor',
  server: 'hotmap.c5h5ouzvh3td.us-east-1.rds.amazonaws.com',
  database: 'Hotmap',  // Nome do seu banco de dados
  options: {
    encrypt: false,  // Use encryption
    enableArithAbort: true,
  },
};
/*
Endpoint: hotmap.c5h5ouzvh3td.us-east-1.rds.amazonaws.com
Porta: 1433
ID: PUCHotMap
Senha: PUCMINASCalor
*/

app.get('/data', async (req, res) => {
  try {
    await sql.connect(dbConfig);
    //const result = await sql.query`SELECT QTD_PES FROM MAIN WHERE NAME = HACKERSPACE`; 
    const result = await sql.query`SELECT QTD_PES FROM MAIN WHERE LAB_ID = 1`; 
    res.json(result.recordset);
  } catch (err) {
    console.error(err);
    res.status(500).send('Erro ao buscar dados');
  }
});

app.listen(port, () => {
  console.log(`Server is running on port ${port}`);
});
